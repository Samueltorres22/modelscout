# Lightweight subset of requirements.txt for the read-only production API
# deploy (see README's "Deploy" section). Deliberately excludes torch,
# transformers, sentence-transformers, huggingface_hub, and langgraph --
# none of the read endpoints (/models, /models/{id}, /runs,
# /observability/summary) touch them, and modelscout/api/main.py only
# imports the pipeline/embeddings modules that need them lazily, inside the
# /pipeline/run and /search handlers, which are disabled via
# ENABLE_ML_FEATURES=false in this deploy. Keeping them out of this file
# entirely (rather than just not calling them) is what keeps the container
# image and RAM footprint small enough for a free-tier host.
#
# Versions pinned to match requirements.txt exactly -- update both together.

annotated-types==0.8.0
anyio==4.14.2
click==8.4.2
fastapi==0.141.1
h11==0.16.0
idna==3.18
numpy==2.5.1
pgvector==0.5.0
psycopg==3.3.4
psycopg-binary==3.3.4
pydantic==2.13.4
pydantic-settings==2.14.2
pydantic_core==2.46.4
python-dotenv==1.2.2
PyYAML==6.0.3
sniffio==1.3.1
starlette==1.4.1
typing-inspection==0.4.2
typing_extensions==4.15.0
uvicorn==0.52.1
